{
  "mcpServers": {
    "playwright": {
      "command": "npx",
      "args": ["@playwright/mcp@latest"],
      "description": "Browser automation — lets Gabriel browse real websites, scrape JS-rendered pages, fill forms",
      "env": {}
    },
    "github": {
      "command": "npx",
      "args": ["-y", "@modelcontextprotocol/server-github"],
      "description": "GitHub — manage colvin-content-os repo, create branches, PRs, issues",
      "env": {
        "GITHUB_PERSONAL_ACCESS_TOKEN": "${GITHUB_TOKEN}"
      }
    },
    "supabase": {
      "command": "npx",
      "args": ["-y", "@supabase/mcp-server-supabase@latest", "--read-only"],
      "description": "Supabase — query leads, review tickets, gabriel_memory directly",
      "env": {
        "SUPABASE_URL": "${NEXT_PUBLIC_SUPABASE_URL}",
        "SUPABASE_SERVICE_ROLE_KEY": "${SUPABASE_SERVICE_ROLE_KEY}"
      }
    },
    "filesystem": {
      "command": "npx",
      "args": [
        "-y",
        "@modelcontextprotocol/server-filesystem",
        "/Users/katrinacolvin/colvin-content-os"
      ],
      "description": "Filesystem — read/write project files directly for Builder Agent tasks"
    },
    "brave-search": {
      "command": "npx",
      "args": ["-y", "@modelcontextprotocol/server-brave-search"],
      "description": "Brave Search — free web search for leads, market research, competitor intel",
      "env": {
        "BRAVE_API_KEY": "${BRAVE_API_KEY}"
      }
    },
    "obsidian": {
      "command": "npx",
      "args": ["-y", "mcp-obsidian", "/Users/katrinacolvin/Colvin-Hermes-Workspace/obsidian"],
      "description": "Obsidian vault — Gabriel reads/writes memory, SOPs, proposals, daily notes"
    },
    "google-calendar": {
      "command": "npx",
      "args": ["-y", "mcp-google-calendar"],
      "description": "Google Calendar — Gabriel sees Alfred's schedule when prioritizing daily actions",
      "env": {
        "GOOGLE_CLIENT_ID": "${GOOGLE_CLIENT_ID}",
        "GOOGLE_CLIENT_SECRET": "${GOOGLE_CLIENT_SECRET}"
      }
    }
  }
}
